# Để trống cả 3 dòng = chạy chế độ local (dữ liệu lưu trong trình duyệt).
# Điền đủ = dùng Supabase (đồng bộ mọi thiết bị). Mật mã màn khóa = mật khẩu của tài khoản VITE_ALLOWED_EMAIL.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ALLOWED_EMAIL=
